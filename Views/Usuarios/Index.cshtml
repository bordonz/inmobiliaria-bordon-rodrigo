@model IEnumerable<inmobiliaria_airbnb.Models.Usuario>
    
@{
	ViewData["Title"] = "Index";
	var rnd = new Random();
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Avatar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
	@foreach (var item in Model) {
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.IdUsuario)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Nombre)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Apellido)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Email)
			</td>
			<td>
				@if(!String.IsNullOrEmpty(item.Avatar)) 
				{
					var x = item.Avatar + "?" + rnd.Next();
					<img width="32" src="@x" />
				}
				else 
				{
					<!--<span>Sin foto</span>-->
					<img width="32" src="https://ui-avatars.com/api/?rounded=true&name=@item.Nombre+@item.Apellido" />
				}

			</td>
			<td>
				@Html.DisplayFor(modelItem => item.RolNombre)
			</td>
			<td>
				<a asp-action="Edit" asp-route-id="@item.IdUsuario" title="Editar"><i class="fa-solid fa-pen"></i></a>&nbsp;
				<a asp-action="Delete" asp-route-id="@item.IdUsuario" title="Eliminar"><i class="fa-solid fa-trash-can text-danger"></i></a>
			</td>
		</tr>
	}
    </tbody>
</table>
<div>
	<button type="button" data-controller="Usuarios" data-action="Index" class="btn btn-primary" data-pagina="@ViewBag.pagina" onclick="cambiarPagina('atras', this)">
		<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left" viewBox="0 0 16 16">
			<path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8"/>
		</svg>
	</button>
	<button type="button" data-controller="Usuarios" data-action="Index" class="btn btn-primary" data-pagina="@ViewBag.pagina" onclick="cambiarPagina('adelante', this)">
		<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-right" viewBox="0 0 16 16">
			<path fill-rule="evenodd" d="M1 8a.5.5 0 0 1 .5-.5h11.793l-3.147-3.146a.5.5 0 0 1 .708-.708l4 4a.5.5 0 0 1 0 .708l-4 4a.5.5 0 0 1-.708-.708L13.293 8.5H1.5A.5.5 0 0 1 1 8"/>
		</svg>
	</button>
</div>